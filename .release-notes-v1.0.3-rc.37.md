# v1.0.3-rc.37 — 보상 후 기존 목표 미션 갱신 방지 시험판

rc.36에서 한 슬롯의 미션 완료·보상을 처리한 뒤, 아직 유지 중인 다른 `모든 몬스터 처치` 미션까지 갱신하던 문제를 수정했습니다. 원인은 보상 후 다섯 슬롯의 상태를 전부 초기화하는 코드였습니다. 진행 중인 목표는 이미 `N/200`이라 새 미션의 `0/200` 조건에 맞지 않아 갱신 대상으로 잘못 분류됐습니다.

이제 **보상을 받은 슬롯 하나만** 새 미션으로 다시 확인합니다. 나머지 목표 슬롯의 확정 상태는 유지하고 갱신 입력을 보내지 않습니다. rc.35 Windows 캡처 경계 수정과 rc.36 완료 미션 인식 수정도 포함됩니다.

`ldmanager-v1.0.3-rc.37-windows-capture-test.zip`을 새 폴더에 전부 압축 해제하고 `ldmanager.exe`를 실행하세요. 기존 rc.36 폴더는 보존하세요. 같은 ADB serial과 LD 창을 선택해 `Test capture`를 통과한 뒤 해당 LD 하나로 먼저 확인하세요.

ZIP SHA-256: `B852896EFF23AE2B1CDCC3B9186518F98C6D44B2CDC7415B8E203A388CA7E32F`

자동 테스트 482개와 Windows 빌드는 통과했습니다. 실제 고객 PC 게임 흐름과 장시간 메모리 결과는 `NEEDS_REAL_TEST`이며 독립 QA 최종 PASS 전의 고객 테스트용 prerelease입니다.
