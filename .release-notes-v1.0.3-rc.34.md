# Windows 캡처 / 커밋 메모리 측정 시험판

**기존 v1.0.3-rc.33은 그대로 유지됩니다. 이 ZIP은 새 폴더에 압축을 풀어 실행하세요.**

이번 버전은 커밋 누수 해결을 확정한 버전이 아니라, ADB 스크린샷을 제거했을 때 메모리 증가가 줄어드는지 확인하기 위한 별도 시험판입니다.

- 운용 중 Windows 캡처만 사용합니다. 실패/오래된 화면/창 종료·최소화·크기 변경 시 해당 LD가 중단되며 ADB 캡처로 돌아가지 않습니다.
- Refresh LD windows에서 정확한 LD 창을 선택 → Test capture → ADB와 Windows 화면이 같은 계정인지 비교 창에서 확인합니다.
- 최초 Test capture의 ADB 스크린샷은 허용되고 별도 집계됩니다. 운용 중 `adb_runtime_captures`는 0이어야 합니다.
- Start All도 각 LD의 검증을 통과해야 동작합니다.
- 30초마다 전체 커밋/한도, LDPlayer·ldmanager·adb 프로세스 메모리, 누적 호출 수를 CSV로 저장합니다.
- 화면 비교가 실패해도 확보한 원본 캡처는 저장합니다.

## 실행

`ldmanager-v1.0.3-rc.34-windows-capture-test.zip` 다운로드 → 별도 폴더에 전체 압축 해제 → `ldmanager.exe` 실행.
Python 설치는 필요 없습니다. 기존 ldmanager를 동시에 돌리지 마세요.
먼저 LD 한 개에서 ADB 매핑, 창 선택, Test capture, Start 순서로 확인하세요.
이후 문제가 재현됐던 동일한 인스턴스 수와 조건으로 측정합니다.

보낼 진단 자료: `diagnostics/memory/<실행별 폴더>/`와 `diagnostics/captures/<LD>/`.
자세한 방법은 ZIP 안 `docs/WINDOWS_CAPTURE_TEST.md`를 확인하세요.

## 검증 범위

자동 테스트(실제 저장된 게임 이미지 회귀 포함), Windows 커밋 수집/CSV 저장, 패키징된 EXE 의존성 점검을 수행했습니다.
개발 PC에는 LDPlayer가 없어 실제 게임 캡처 호환성·다계정 장시간 안정성·커밋 개선 효과는 아직 미검증입니다.
