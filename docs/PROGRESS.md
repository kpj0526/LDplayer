# Progress

## 2026-09-14 — 고객 참고 영상 요구사항 병합

- 기존 프로젝트를 유지한 채 고객 영상의 자유 토벌작전 5슬롯 전체 순환을 MVP 필수 범위로 병합했다.
- `PROJECT_SPEC.md`, `TASK_PACKET.md`, `TEST_PLAN.md`, `ACCEPTANCE_STATUS.md`를 새로 추가해 범위, 구현·QA 전달 사항, 실환경 게이트, AC-61~90(총 90개)을 기록했다.
- 기존 구현 점검: 계정별 Worker, 명시적 단일 ADB serial 캡처/탭, 제한된 재시도, 5슬롯 상태 머신은 존재한다.
- 차이/차단점: 앱은 `PlaceholderRecognizer`를 사용한다. OpenCV/OCR, 실제 템플릿/ROI/좌표 보정, 실제 LDPlayer/게임 전체 흐름 검증은 아직 없다.

## 현재 단계

**실제 화면 인식 구현 전 단계.** 실게임 매크로는 아직 실행 가능 상태가 아니다. fake 테스트로 검증된 제어 흐름을 실제 화면 판별로 교체하고, 사용자 제공 환경에서 단일 계정 감독 시험 → 9계정 격리 시험 순으로 진행해야 한다.

## 다음 작업

1. 사용자가 무편집 원본 화면 캡처/영상(임무 목록, 목표/비목표 상세, 모든 비용의 갱신 팝업, 0~199/200·200/200/완료, 보상·결과·복귀)과 실제 화면 해상도를 제공한다.
2. code가 그 자료로 OpenCV/OCR recognizer, 템플릿, ROI, 화면 변화/정체 검증을 구현한다.
3. qa가 신규 AC-61~90 자동·실환경 검증을 수행한다.
4. LD1 한 계정의 감독 전체 순환이 확인된 뒤 LD1~LD9 동시 실행과 계정 간 입력 격리를 검증한다.
