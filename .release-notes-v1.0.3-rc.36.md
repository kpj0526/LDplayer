# v1.0.3-rc.36 — 완료된 목표 미션 인식 수정 시험판

rc.35에서 `Windows capture verified` 후 Start하면 `refresh_popup_not_verified`가 뜨던 사례를 수정했습니다. 고객 화면에는 갱신 확인창이 아니라 `모든 몬스터 처치` 목표 미션의 `완료` 버튼이 있었습니다. 기존 코드는 새 미션의 `0/200`만 수락 조건으로 검사해, 이미 완료된 목표를 갱신 대상으로 분류하고 존재하지 않는 갱신창을 기다렸습니다.

이제 완료 버튼과 목표 문구를 확인한 뒤 기존의 검증된 완료·보상 경로로 진행합니다. 완료된 다른 미션이거나 목표 인식이 불확실하면 갱신·보상 입력을 보내지 않고 중단합니다. rc.35의 Windows 캡처 경계 수정도 포함됩니다.

`ldmanager-v1.0.3-rc.36-windows-capture-test.zip`을 새 폴더에 전부 압축 해제하고 `ldmanager.exe`를 실행하세요. 기존 rc.35 폴더는 보존하세요. ADB serial과 같은 LD 창을 선택해 `Test capture`를 통과한 다음, 우선 해당 LD 하나만 Start해 보세요.

ZIP SHA-256: `3B240250EED9F53675480B8ABE4E4C8297951478055371D88985609E0DFBF5FC`

자동 테스트 482개와 Windows 빌드는 통과했습니다. 실제 고객 PC 게임 흐름과 장시간 메모리 결과는 `NEEDS_REAL_TEST`이며 독립 QA 최종 PASS 전의 고객 테스트용 prerelease입니다.
