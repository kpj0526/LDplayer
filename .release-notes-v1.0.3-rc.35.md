# v1.0.3-rc.35 — Windows 캡처 경계 오류 수정 시험판

rc.34에서 `Test capture`가 `Game viewport is outside the captured window`로 실패하던 사례를 수정했습니다. LDPlayer 창의 상단 메뉴와 오른쪽 도구 막대가 함께 캡처되고, 게임 화면이 창의 아래쪽 또는 오른쪽 끝에 닿을 때 잘라낼 영역이 1픽셀 넘을 수 있었습니다. 게임 화면 영역의 양쪽 끝을 각각 계산하고 캡처 범위 안으로 제한합니다. LD1~LD9가 같은 경로를 사용합니다.

`ldmanager-v1.0.3-rc.35-windows-capture-test.zip`을 **새 폴더에 전부 압축 해제**하고 `ldmanager.exe`를 실행하세요. 기존 rc.34 폴더는 보존하세요. `Refresh ADB devices`에서 해당 LD의 serial을 저장하고, `Refresh LD windows`에서 같은 LDPlayer 창을 선택한 뒤 게임 미션 목록 화면에서 `Test capture`를 실행하세요. 비교 창에서 ADB와 Windows 이미지가 같은 계정인지 확인한 뒤 해당 LD 하나만 Start하세요.

ZIP SHA-256: `9BFEBC2699BA2713D3DC8BAB122D7827E5D55E279D778CACA4467A9E1A04DB49`

자동 테스트 480개와 Windows 빌드는 통과했습니다. 고객 PC에서의 실제 LDPlayer 캡처, 장시간 운용 및 커밋 메모리 개선은 아직 `NEEDS_REAL_TEST`입니다. QA 독립 검증도 완료 전이므로 최종 PASS 버전이 아닌 고객 테스트용 prerelease입니다.
